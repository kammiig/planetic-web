*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# NOTE: public/build is deliberately COMMITTED — the cPanel server has no
# Node, so deploys ship the locally-built assets. Run `npm run build` and
# commit the result whenever CSS/JS changes.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Project build artifacts
/docs/_extracted
.DS_Store
